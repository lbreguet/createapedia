<div data-id="{{article.id}}">
  <h3 class="title">{{article.title}}</h3>
  <p>{{article.body}}</p>
  <div>
    <div data-id = "{{article.id}}" class="btn-group" id="article-destroy">
      <button type="submit" class="btn btn-danger">
      Delete
      </button>
    </div>
  </div>
</div>
