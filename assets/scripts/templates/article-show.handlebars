{{#each article as |article|}}

  <div data-id="{{article.id}}">
    <h3>{{article.title}}</h3>
    {{#if article.description}}
      <li>{{article.description}}</li>
    {{else}}
      <li></li>
    {{/if}}
      <p>{{article.body}}</p>
  </div>

{{/each}}
